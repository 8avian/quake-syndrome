{% extends 'layout.html.twig' %}

{% block title %}Content{% endblock %}

{% block content %}
    {% for article in articles %}
        <h2>{{ article.title }}</h2>
        <p>{{ article.body }}</p>
    {% endfor %}
{% endblock %}